{
  "name": "@bitc/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Phase 4: react-router build' && mkdir -p build/server && echo '' > build/server/index.js",
    "dev": "node --env-file-if-exists=../../.env.local src/server.ts",
    "start": "node src/server.ts"
  },
  "dependencies": {
    "@bitc/agent": "workspace:*",
    "@bitc/core": "workspace:*",
    "@bitc/db": "workspace:*",
    "@bitc/models": "workspace:*",
    "@bitc/rag": "workspace:*",
    "@bitc/shopify": "workspace:*",
    "zod": "4.5.4"
  }
}
