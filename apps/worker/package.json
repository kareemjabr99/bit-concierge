{
  "name": "@bitc/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node24 --packages=external --outfile=dist/index.js",
    "dev": "node --experimental-strip-types --watch src/index.ts"
  },
  "dependencies": {
    "@bitc/core": "workspace:*",
    "@bitc/db": "workspace:*",
    "pg-boss": "12.30.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
