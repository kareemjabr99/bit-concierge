node_modules/
dist/
build/
.react-router/
coverage/
*.tsbuildinfo

# Secrets — only .env.example is ever tracked
.env
.env.*
!.env.example

.DS_Store
*.log
.pnpm-store/
.drizzle-generated/
.embed-cache/
.rerank-cache/

# Local preview config. Points at whatever Node 24 this machine has; .nvmrc is
# the committed answer to "which node", and this file is the machine's own.
.claude/launch.json
apps/widget/dist/
.shopify-token-cache/
